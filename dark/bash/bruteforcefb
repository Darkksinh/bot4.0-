const bruteforcefb = (prefix) => { 
	return `
━━━━━━━━━━━━━━━━━━━━
Facebash – Brute Force Facebook Termux
━━━━━━━━━━━━━━━━━━━━
git clone https://github.com/thelinuxchoice/facebash

$ cd instashell

$ chmod +x facebash.sh

$ service tor start

$ sudo ./facebash.sh

Install requirements:

$ chmod +x install.sh

$ sudo ./install.sh

*💎 NOTA:*
*Use scripts e bots com sabedoria!*
*Se você usar indevidamente o Script e o proprietário do bot não será responsável!*
`
}
exports.bruteforcefb = bruteforcefb
